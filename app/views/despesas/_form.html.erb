<%= form_with(model: despesa, local: true) do |form| %>
  <% if despesa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(despesa.errors.count, "error") %> prohibited this despesa from being saved:</h2>

      <ul>
        <% despesa.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :numero_doc %>
    <%= form.text_field :numero_doc %>
  </div>

  <div class="field">
    <%= form.label :fornecedor %>
    <%= form.text_field :fornecedor %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
