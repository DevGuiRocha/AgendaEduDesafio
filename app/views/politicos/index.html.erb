<!--<p id="notice"><%= notice %></p>

<h1>Politicos (CE)</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Cpf</th>
      <th>Sguf</th>
      <th>Sgpartido</th>
      <th>Id cadastro</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @politicos.each do |politico| %>
      <tr>
        <td><%= politico.nome %></td>
        <td><%= politico.cpf %></td>
        <td><%= politico.sguf %></td>
        <td><%= politico.sgpartido %></td>
        <td><%= politico.id_cadastro %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

-->

<div class="container">
  <table class="table">
    <thead class="thead-dark">
      <% if @apresenta.politicos.size == 0%>,
        <p>Nenhum Politico encontrado</p>
      <% else %>
        <h2>Politicos Cadastrados (CE)</h2> 

          <%== pagy_bootstrap_nav(@pagy) %>

          <tr>
            <th>Foto </th>
            <th>Nome </th>
            <th>CPF </th>
            <th>Partido </th>
            <th>Total </th>
          </tr>

          <% @records.each do |politicos|%>

            <tr>
              <td><%= link_to foto_politico(politico), politico_path(politico) %></td>
              <td><%= link_to "#{politico.nome}", politico_path(politico) %></td>
              <td><%= politico.cpf %></td>
              <td><%= politico.sgpartido %></td>
              <td><%= number_to_currency(@apresenta.gastos[politico.id], :unit => "R$") %></td>
            </tr>

          <% end %>
      <% end %>
  </table>
    <%== pagy_bootstrap_nav(@pagy) %>
</div>
